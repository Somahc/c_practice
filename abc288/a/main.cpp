#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i<(n); i++)
using namespace std;

int main(){
    int n;
    cin >> n;
    rep(i,n){
        long long int a, b;
        cin >> a >> b;
        cout << a + b << endl;
    }
    return 0;
}
