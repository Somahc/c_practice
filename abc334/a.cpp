#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i<(n); i++)
using namespace std;

int main(){
    int b,g;
    cin >> b >> g;
    if(b > g){
        cout << "Bat" << endl;
        return 0;
    }
    cout << "Glove" << endl;
    return 0;
}